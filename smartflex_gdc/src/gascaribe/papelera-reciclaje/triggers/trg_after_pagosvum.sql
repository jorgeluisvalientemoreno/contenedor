DECLARE
  nuConta NUMBER;
BEGIN
SELECT COUNT(1) INTO nuConta
  FROM DBA_OBJECTS
  WHERE OBJECT_NAME = 'TRG_AFTER_PAGOSVUM'
   AND OWNER = 'OPEN'
   AND OBJECT_TYPE='TRIGGER';

  IF nuConta >= 1 THEN
      EXECUTE IMMEDIATE 'DROP TRIGGER OPEN.TRG_AFTER_PAGOSVUM';
  END IF;
END;
/