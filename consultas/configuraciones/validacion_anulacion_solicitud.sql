SELECT *
FROM OPEN.GE_PERSON
WHERE NAME_ LIKE '%BOLA%'


 SELECT GP.PERSON_ID, GP.ORGANIZAT_AREA_ID
   
      FROM OPEN.GE_PERSON GP
     INNER JOIN OPEN.GE_ORGANIZAT_AREA AO
        ON GP.ORGANIZAT_AREA_ID = AO.ORGANIZAT_AREA_ID
     WHERE /*PERSON_ID =15977
       AND */AO.ORGANIZAT_AREA_TYPE = 10;
   
 

      SELECT COUNT(1), R.MEDIO_USO
        FROM OPEN.LDC_REP_AREA_TI_PA_CA R
       INNER JOIN OPEN.PS_PACKAGE_TYPE PS
          ON R.PACKAGE_TYPE_ID = PS.PACKAGE_TYPE_ID
       WHERE R.PACKAGE_TYPE_ID=100101
       GROUP BY MEDIO_USO;
       
         SELECT ORGANIZAT_AREA_ID
        FROM OPEN.LDC_REP_AREA_TI_PA_CA R
       INNER JOIN OPEN.PS_PACKAGE_TYPE PS
          ON R.PACKAGE_TYPE_ID = PS.PACKAGE_TYPE_ID
       WHERE R.PACKAGE_TYPE_ID  = 100101;
       
select  *
from open.ld_parameter
where parameter_id='AREAS_SERCLIENTE'
       
       
